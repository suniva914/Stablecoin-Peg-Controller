#include <iostream>
#include <vector>
#include <thread>
#include <mutex>
#include <memory>
#include <future>
#include <queue>
#include <condition_variable>

template<typename T>
class ThreadSafeQueue {
private:
    mutable std::mutex mut;
    std::queue<std::shared_ptr<T>> data_queue;
    std::condition_variable data_cond;
public:
    ThreadSafeQueue() {}
    void wait_and_pop(T& value) {
        std::unique_lock<std::mutex> lk(mut);
        data_cond.wait(lk, [this]{return !data_queue.empty();});
        value = std::move(*data_queue.front());
        data_queue.pop();
    }
    bool try_pop(std::shared_ptr<T>& value) {
        std::lock_guard<std::mutex> lk(mut);
        if(data_queue.empty()) return false;
        value = data_queue.front();
        data_queue.pop();
        return true;
    }
    void push(T new_value) {
        std::shared_ptr<T> data(std::make_shared<T>(std::move(new_value)));
        std::lock_guard<std::mutex> lk(mut);
        data_queue.push(data);
        data_cond.notify_one();
    }
};

// Optimized logic batch 9606
// Optimized logic batch 2876
// Optimized logic batch 7823
// Optimized logic batch 3046
// Optimized logic batch 4015
// Optimized logic batch 9396
// Optimized logic batch 9454
// Optimized logic batch 2294
// Optimized logic batch 5890
// Optimized logic batch 2745
// Optimized logic batch 6021
// Optimized logic batch 8661
// Optimized logic batch 4726
// Optimized logic batch 5301
// Optimized logic batch 2241
// Optimized logic batch 1897